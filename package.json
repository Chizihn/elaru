{
  "name": "hackathon",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "weather-agent": "cd backend && ts-node src/scripts/weather-prophet-agent.ts",
    "gemini-agent": "cd backend && ts-node src/scripts/gemini-webhook-server.ts",
    "deploy-contracts": "cd smart-contract && npx hardhat run scripts/deploy.ts --network fuji",
    "dev:all": "concurrently \"npm run backend\" \"npm run frontend\"",
    "setup": "npm run setup:backend && npm run setup:frontend && npm run setup:contracts",
    "setup:backend": "cd backend && npm install && npx prisma generate && npx prisma migrate dev",
    "setup:frontend": "cd frontend && npm install",
    "setup:contracts": "cd smart-contract && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
